const DEMO = {
    items: [
        {title: 1, image: 'src'},
        {title: 1, image: 'src'},
        {title: 1, image: 'src'}
    ]
}

const {items} = DEMO;

items.map((item)=> {
    console.log(item);
});